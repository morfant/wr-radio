# WR-Radio 설계노트

## 프로젝트 개요
- **목적**: 전 세계 환경음을 스트리밍하는 휴대용 인터넷 라디오
- **핵심 하드웨어**: Raspberry Pi Zero 2W, ST7789 240×240 LCD, UDA1334A I2S DAC, PAM8403 앰프, ADS1115 ADC, 로터리 인코더
- **스트리밍 채널**: 제주, 런던, 뉴욕, 캘리포니아, 후지산 (5개 채널)
- **프로토타입 수량**: 약 5대

---

## 전원 시스템

### 아키텍처: TP4056 + TPS61023 분리 구성
```
USB-C → [TP4056] → LiPo 배터리 (2000~3000mAh)
                         │
                    [OUT+/OUT-]
                         │
                    [TPS61023 VIN] → 5V OUT → Pi 5V
                         │
                    EN 핀 ← 슬라이드 스위치 (GND 연결/해제)
```

- TP4056 OUT 단자 사용 (BAT+ 직결 아님) → DW01 과방전 보호 커버
- TPS61023: 3A 출력, Pi 부팅 피크 전류 헤드룸 충분
- USB 핫스왑 드롭아웃 없음 확인 (throttled=0x0)
- 슈퍼캡 불필요 (LiPo Rider Plus 때와 달리 드롭아웃 자체가 없음)

### MP2639A 불채택 사유
- TP4056로 이미 전체 검증 완료
- 1A 충전이면 야간 충전 사용 패턴에 충분
- 추가 복잡도 대비 이득 없음

### 전원 제어
- **슬라이드 스위치**: TPS61023 EN 핀을 GND에 연결/해제
- **택트 스위치**: 설계에서 제거됨 (슬라이드 스위치 단독 운용)
- **Sleep/Wake**: 제거됨 — overlayfs로 안전한 하드 전원 차단 가능

### 배터리 모니터링 (ADS1115)
- **A0**: 배터리 전압 — TPS61023 VIN에서 10kΩ/10kΩ 분압 → 소프트웨어 ×2 보정
- **A1**: VBUS 감지 — TP4056 USB VBUS 패드에서 10kΩ/10kΩ 분압
- 충전 상태 판별:
  - VBUS 있음 + BAT < 4.2V → 충전 중
  - VBUS 있음 + BAT ≈ 4.2V → 충전 완료
  - VBUS 없음 → 배터리 구동 중
- ADS1015 (12비트) 도 이 용도에는 충분 — 차기 리비전에서 비용 절감 가능

---

## PCB 설계 (Eagle CAD / Fusion 360 Electronics)

### 형태
- Pi Zero 2W와 수평 배치 (적층 아닌 옆으로 나란히)
- Pi 쪽: 직각 male 핀헤더
- PCB 쪽: 직각 female 소켓 (2x20 right angle female header)

### 높이 축소 계획
- **로우 프로파일 헤더 소켓 주문**: PH3.5mm (일반 PH8.5 대비 5mm 절감)
  - E-Simpo 1x40핀, AliExpress 주문 완료
  - 매칭 male 헤더: PH7.5mm 주문 완료
- 모듈 스태킹 검토 중 (UDA 아래 PAM8403 등) — 발열/노이즈 주의 필요

### 그라운드 설계
- 바텀 레이어: GND 폴리곤 (3개 그룹 분할)
- 탑 레이어: 여분 공간 GND 구리 푸어
- Star grounding: 아날로그/디지털 GND 한 점 합류

### 디커플링 캐패시터 (최종 목록)
- 각 모듈 VIN-GND: 100nF 세라믹 (대부분 모듈 내부 디커플링 있으므로 보험용)
- 3.3V GPIO 헤더 근처: 100nF + 10µF
- 로터리 인코더 S1/S2: 100nF 각각 (소프트웨어 디바운싱 충분하면 생략 가능)

---

## PCB 프로토타입 테스트 결과 (2026-04-30)

### ✅ 정상 동작 확인
- LCD (ST7789) 정상 표시
- 배터리 전압 모니터링 (ADS1115 A0) 정상
- VBUS 충전 상태 감지 (ADS1115 A1) 정상
- BLE 오디오 스트리밍 정상
- GND-5V 간 쇼트 없음 (캐패시터 충전으로 인한 일시적 도통만 확인 — 정상)

### ❌ BCLK 배선 오류 발견 및 수정
- **문제**: I2S BCLK가 GPIO 15에 연결됨 (정상: GPIO 18)
- **원인**: PCB 설계 시 핀 배정 오류
- **임시 수정**: 커터칼로 GPIO 15→UDA1334A BCLK 트레이스 절단, GPIO 18에서 점퍼 와이어로 연결
- **결과**: UDA1334A 오디오 출력 정상 동작
- **다음 리비전**: PCB에서 BCLK를 GPIO 18로 수정 필수
- **교훈**: Pi I2S 핀은 하드웨어 고정 — BCLK=GPIO18, LRCLK=GPIO19, DIN=GPIO21, 소프트웨어로 변경 불가

### 🔊 스피커 노이즈 분석
두 가지 노이즈 원인 확인:

**1. PAM8403 자체 노이즈 (Class-D 스위칭)**
- 이어폰 잭: 깨끗함
- 스피커: 저볼륨/무음 시 노이즈 들림, 스트리밍 중에는 묻힘
- 원인: PAM8403 고정 24dB 게인이 플로어 노이즈 증폭
- 분압 저항 기존 설치되어 있으나 불충분
- 소프트웨어 볼륨 낮추기로는 해결 불가 (앰프 자체 노이즈이므로)
- **대응**: 프로토타입에서는 감수, 차기 리비전에서 MAX98357A (I2S 직결 앰프) 교체 검토

**2. LCD SPI 노이즈**
- LCD 업데이트 시 "디기딕" 하는 추가 노이즈 발생
- SPI 고주파 노이즈가 전원 라인을 통해 오디오 쪽으로 전달
- **수정**: 메인 5V와 LCD VCC 사이에 **페라이트 비드** 직렬 삽입
  ```
  메인 5V ── [페라이트비드] ── 100nF캡 ── LCD VCC
                                  |
                                 GND
  ```
- 페라이트 비드 사양: 100Ω @ 100MHz, 정격전류 500mA 이상
- **결과**: 완전히 잡히진 않았지만 개선 효과 있음 확인
- **다음 리비전**: PCB에 페라이트 비드 정식 포함

### 기타 참고
- 다른 Pi에 기존 SD카드 이동 시 PulseAudio 캐시 충돌 가능
  - `rm -rf ~/.config/pulse && pulseaudio -k && pulseaudio --start` 로 해결
- `amixer contents` 빈 출력은 정상 (UDA1334A에 하드웨어 볼륨 컨트롤 없음)

---

## 소프트웨어

### 아키텍처
- Python 모듈 구조: main.py, display.py, player.py, config.py, state.py, input.py
- 오디오: mpv (스트리밍), BlueZ + PulseAudio (Bluetooth)
- WiFi: NetworkManager / nmcli (cloud-init/netplan에서 이전 완료)
- 디스플레이: SPI 16MHz (64MHz에서 노이즈로 인해 하향)

### 부팅 최적화
- cloud-init, ModemManager 비활성화
- 39초 → 34초로 단축
- 스플래시 이미지 + systemd 서비스 구현

### UI
- 로터리 인코더: 짧게 누르기 = 볼륨 모드, 길게 누르기 = 밝기 모드
- LCD 업데이트: 부분 화면 렌더링 (region-specific update)
- 날씨 아이콘: PIL 커스텀 드로잉 (이모지 폰트 호환 문제 해결)

---

## SD카드 보호: overlayfs (배포판 적용 예정)

- 루트 파일시스템을 읽기 전용 마운트, 변경사항은 RAM에만 기록
- 전원 갑자기 끊어도 SD카드 손상 없음
- 영구 저장 항목 (별도 `/data` 파티션): config.json, Bluetooth 페어링 정보
- 개발 중에는 OFF, 배포판 최종 이미지에서 ON

---

## 케이스 (Fusion 360)

- 파라메트릭 Python 스크립트로 자동 생성
- 보스, 리브, 마운팅 시스템, 컴포넌트별 컷아웃 포함
- 대형 배터리 + 스피커 레이아웃 최적화 중

---

## 스피커 현황

- 현재: 102mm 4Ω — 저음 왜곡 + SPI 노이즈 (PCB 이후 재평가)
- 검토 중: 30mm 캡슐 스피커, 익사이터(surface transducer)

---

## 주요 학습 사항

- 브레이크아웃 모듈은 내부 디커플링 포함 — 외부 추가는 보험용
- SPI 64MHz 노이즈가 전원/GND를 오염 → 16MHz 하향 + LCD VCC 캡 + 페라이트 비드로 완화
- PAM8403 고정 24dB 게인 → 입력 어테뉴에이션 또는 앰프 교체 필요
- UDA1334A는 라인 레벨 출력 → 이어폰 직결은 기능하지만 최적은 아님
- LCD update를 인코더 인터럽트 핸들러 안에서 하면 타이밍 간섭 → 디바운스 분리 필요
- LiPo 최적 범위 20~80%, 완전 방전 후 충전은 NiCd 시대 미신
- Pi I2S 핀은 SoC 하드웨어 고정: BCLK=GPIO18, LRCLK=GPIO19, DIN=GPIO21 — 변경 불가

---

## 향후 계획

- [x] TP4056 + TPS61023 브레드보드 검증 완료
- [x] USB 핫스왑 드롭아웃 없음 확인
- [x] 전체 부하 테스트 (Pi + DAC + 앰프 + LCD) 완료
- [x] ADS1115 배터리 전압 + VBUS 감지 구현
- [x] 부팅 시간 최적화 (39초 → 34초)
- [x] 스플래시 이미지 + systemd 서비스 구현
- [x] PCB Rev.1 제작 및 기본 동작 확인
- [x] BCLK 배선 오류 발견 및 점퍼 와이어 수정
- [x] SPI 노이즈 완화: 페라이트 비드 삽입 테스트
- [ ] **PCB Rev.2 설계** — BCLK→GPIO18 수정, 페라이트 비드 정식 포함
- [ ] 로우 프로파일 헤더 입고 후 높이 테스트 (PH3.5 소켓 + PH7.5 핀)
- [ ] 스피커 선정 확정 (PCB 이후 노이즈 재평가)
- [ ] 케이스 최종 설계 완성 + 출력
- [ ] overlayfs 설정 (배포판 단계)
- [ ] SD카드 이미지 배포판 제작 (의존성 포함)
- [ ] 사용자용 설정 가이드 문서화
- [ ] Bluetooth 헤드폰 지원 (선택적 확장)
- [ ] 마이크 입력 / 필드 레코딩 (장기)
- [ ] 앰프 교체 검토: MAX98357A (I2S 직결, UDA1334A+PAM8403 대체 가능)

---

## 주요 부품 공급처

- 디바이스마트, 아이씨뱅큐 (국내)
- Adafruit, AliExpress (해외)
- 로우 프로파일 헤더: AliExpress E-Simpo

---

*최종 업데이트: 2026-04-30 (PCB Rev.1 테스트: BCLK 배선 오류 수정, SPI 노이즈 페라이트 비드 완화, PAM8403 자체 노이즈 확인, 로우 프로파일 헤더 주문)*
