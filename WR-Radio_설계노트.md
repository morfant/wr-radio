# WR-Radio 설계 노트

> 새 채팅 세션 시작 시 이 파일을 붙여넣으면 맥락이 바로 복원됩니다.

---

## 프로젝트 개요

Raspberry Pi Zero 2W 기반 인터넷 라디오. 전통적인 라디오 조작감을 재현하면서 전세계 5개 지역의 환경음(ambient sound)을 스트리밍하는 휴대용 배터리 구동 기기.

**스트리밍 지역**
- Jeju Georo (제주, 한국)
- London Stave Hill (런던, 영국)
- New York Wave Farm (뉴욕, 미국)
- Jasper Ridge (캘리포니아, 미국)
- Mt. Fuji Forest (후지산, 일본)

날씨 정보(OpenWeatherMap API)를 각 지역별로 함께 표시하여 몰입감 강화.

---

## 하드웨어 구성

| 컴포넌트 | 모델 | 역할 |
|---------|------|------|
| SBC | Raspberry Pi Zero 2W | 메인 컴퓨터 |
| 디스플레이 | ST7789 240x240 LCD | UI 표시 |
| 입력 | 로터리 엔코더 | 채널/볼륨 조작 |
| DAC | UDA1334A (I2S) | 디지털→아날로그 변환 |
| 앰프 | PAM8403 | 스피커 구동 |
| 헤드폰 잭 | PJ-307C (스위치드) | 헤드폰/스피커 자동 전환 |
| 배터리 | 10000mAh LiPo | 전원 |
| 충전 IC | TP4056 (DW01 보호회로 포함) | 충전 관리 |
| 부스트 컨버터 | TPS61023 | 배터리→5V 변환 |
| ADC | ADS1115 | 배터리 전압 + VBUS 감지 |
| 전원 스위치 | 슬라이드 스위치 (TPS61023 EN 핀) | 하드 전원 on/off |
| 케이스 | 커스텀 3D 프린팅 (Fusion 360) | 하우징 |

---

## GPIO 핀 맵 (BCM 번호 기준)

### 전체 요약

| 모듈 | 신호 | BCM 핀 | 인터페이스 | 비고 |
|------|------|--------|-----------|------|
| **로터리 엔코더** | S1 | 17 | GPIO (PUD_UP) | |
| | S2 | 27 | GPIO (PUD_UP) | |
| | BTN | 22 | GPIO (PUD_UP) | |
| **ST7789 LCD** | CS | 26 | SPI0 CE | |
| | DC | 13 | GPIO OUT | |
| | RST | 6 | GPIO OUT | |
| | BL | 12 | PWM0 (1kHz) | 백라이트 밝기 |
| **UDA1334A** | WSEL (LRCLK) | 19 | I2S (PCM_FS) | 커널 처리 |
| | BCLK | 18 | I2S (PCM_CLK) | 커널 처리 |
| | DIN | 21 | I2S (PCM_DOUT) | 커널 처리 |
| **PAM8403** | STBY | 24 | GPIO OUT | LOW=대기, HIGH=동작 |
| **헤드폰 감지** | DETECT | 23 | GPIO IN (PUD_UP) | PJ-307C 스위치 접점 |
| **ADS1115** | SDA | 2 | I2C1 | 배터리 전압 + VBUS 감지 |
| | SCL | 3 | I2C1 | 배터리 전압 + VBUS 감지 |

### ADS1115 채널 할당

| 채널 | 용도 | 회로 | 비고 |
|------|------|------|------|
| A0 | 배터리 전압 측정 | TPS61023 VIN → 10kΩ/10kΩ 분압 → A0 | 측정값 × 2 = 실제 전압 |
| A1 | VBUS 감지 (충전 상태) | TP4056 VBUS 패드 → 10kΩ/10kΩ 분압 → A1 | ≈2.5V = USB 연결, 0V = 배터리 구동 |

- I2C 주소: 기본값 (ADDR → GND, 0x48)

### 충전 상태 판별 로직

| VBUS (A1) | 배터리 전압 (A0) | 상태 |
|-----------|-----------------|------|
| ≈2.5V | < 4.2V | 충전 중 |
| ≈2.5V | ≈ 4.2V | 충전 완료 |
| 0V | - | 배터리 구동 |

### 헤드폰 감지 동작

```
헤드폰 미삽입: DETECT = LOW  → PAM8403 STBY HIGH (앰프 ON)
헤드폰 삽입:   DETECT = HIGH → PAM8403 STBY LOW  (앰프 OFF)
```

소프트웨어(`player.py`)에서 GPIO 23 폴링으로 감지, 50ms 디바운스 적용.

---

## 전원 시스템

### PCB 구성 (확정)

```
USB → [TP4056 + DW01 보호] → LiPo 배터리
                LiPo 배터리 → [TPS61023 부스트] → 5V → Pi
```

- **충전 IC와 부스트 컨버터 분리 설계** 채택
- TPS61023은 항상 LiPo에서만 전원 공급 → USB 유무와 무관, 전환 드롭아웃 없음
- 슈퍼캡 홀드업 회로 불필요 (드롭아웃 자체가 없음)
- **브레드보드 검증 완료 (2026-03-21)**: 5.12V 출력, USB 핫스왑 드롭아웃 없음, throttled=0x0 확인

### 충전 IC: TP4056 선택 이유
- 회로 단순, 브레드보드 검증 완료
- DW01 보호회로 포함 (과방전/과충전/단락 보호)
- 최대 1A 충전 (10000mAh 기준 약 10시간 — 야간 충전으로 충분)
- 선형 방식이라 발열 있음 (1A 충전 시 약 0.8W) → PCB에서 방열 패드로 대응
- ~~MP2639A~~ 검토 후 불채택: 스위칭 방식으로 발열 적고 2A 충전 가능하나, 회로 복잡하고 새로 검증 필요. 실용적 이점 대비 복잡도 증가 부적합

### 부스트 컨버터: TPS61023 선택 이유
- 최대 3A 출력 → Pi 부팅 피크 전류 대응 (MT3608은 2A로 마진 부족)
- 저입력 전압에서도 안정적
- 브레드보드 검증 완료: 5.12V 안정 출력

### 전원 스위치 (확정)
- **슬라이드 스위치 단독** — TPS61023 EN 핀 제어
- ~~택트 스위치 제거~~ — sleep/wake 기능 불채택, 슬라이드 스위치만으로 on/off
- EN 핀 전류 거의 0 → 소형 저전류 스위치로 충분
- 전원 OFF = 슬라이드 스위치 내림 (EN → GND → 부스트 차단 → 자연 방전 후 Pi 전원 완전 차단)
- 전원 ON = 슬라이드 스위치 올림 → Pi 부팅 (약 34초)
- overlayfs 적용 시 전원 갑자기 끊어도 SD카드 손상 없음

### LiPo Rider Plus 고장 경위 (2026-03-05)
- 증상: 배터리 4.1V 충분한데 부팅 불가, Pi 전원 핀에서 부팅 시 4.3V까지 강하
- 원인: 부스트 컨버터 출력 약화 (열화), 배터리→5V 부스트 경로만 문제 (USB 입력 시 5.1V 정상)
- 추정 원인: 슈퍼캡 인러시 전류 반복 스트레스 및 불안정한 전원 사이클 누적
- **결론: LiPo Rider Plus는 PCB에서 제외, 분리 구성으로 전환**

### 전원 안정화
- Pi 메인 전원 라인: **페라이트 비드 제거** (고전류 라인 부적합, WiFi 피크 전류 시 전압강하 유발)
- Pi 전원 바로 옆: **220µF 캐패시터** (부팅 피크 전류 버퍼링)

### 배터리 관리 원칙
- 최적 사용 범위: **20~80% 충전** 유지 (수명 3배 연장)
- LiPo는 완전 방전/완전 충전 반복이 수명에 해로움 (NiCd와 반대)
- DW01 보호회로가 약 2.4V 이하에서 출력 차단 (하드웨어 과방전 보호)

---

## 오디오 시스템

### 신호 경로
```
Pi (I2S) → UDA1334A DAC → [전압 분배기] → PJ-307C 잭
                                              ├── 헤드폰 (직결)
                                              └── PAM8403 앰프 → 스피커
```

### UDA1334A 연결
- VIN: 5V (온보드 레귤레이터가 3.3V로 변환)
- 3VO: 모듈 출력 3.3V (다른 부품 공급용)
- `config.txt`에 `dtoverlay=hifiberry-dac` 적용
- `aplay -l` 에서 `card 0: sndrpihifiberry` 로 인식 확인

### 헤드폰/스피커 자동 전환
- PJ-307C의 물리적 스위치 접점 이용 (소프트웨어 불필요)
- 헤드폰 삽입 시 스피커 회로 자동 차단

### 노이즈 대책 (중요)
- 헤드폰 출력: **AGND** 연결로 디지털 노이즈 최소화
- 앰프 GND와 DAC AGND 분리 (그라운드 오염 방지)
- 전압 분배기: 클리핑 방지용

### 페라이트 비드 + 캐패시터 필터

**위치 우선순위**
1. UDA1334A VIN (DAC 전원) — 오디오 품질 직결, 최우선
2. PAM8403 VCC (앰프 전원) — 스피커 험 억제
3. LCD 전원 — LCD 스위칭 노이즈 차단

```
5V ──[페라이트 비드]──┬──── VIN (DAC/앰프)
                     ├── 10µF 전해/탄탈 (저주파 안정화)
                     └── 100nF 세라믹 (고주파 바이패스)
                     │
                    GND
```

**주의**: 페라이트 비드는 DC 저항 있음 → Pi 메인 전원 같은 고전류 라인엔 부적합.

---

## LCD SPI 노이즈 문제 (미해결 → PCB에서 근본 해결 예정)

### 현상
- 프로그램 실행 중 LCD 백라이트 밝기 출렁거림
- 스피커에서 디지털 노이즈 (헤드폰은 깨끗함 → DAC는 무관, PAM8403 경로 문제)
- **LCD를 끄면 노이즈 완전히 사라짐 → SPI 전송이 주범 확정**

### 현재 적용한 완화책 (소프트웨어)
- SPI 속도 64MHz → **16MHz** 낮춤
- 애니메이션 업데이트 주기 늘림
- 부분 업데이트(`display_image_region`) 적극 활용 → 전체 화면 전송 최소화

### 현재 적용한 완화책 (하드웨어)
- LCD VCC에 100nF 세라믹 캐패시터 → GND
- PAM8403 VCC에 페라이트 비드 + 100µF + 100nF

### PCB 설계 시 근본 해결 포인트
- GND 플레인 전체 깔기 (루프 면적 최소화)
- SPI 라인(BCM 26/13/6)과 아날로그 신호선 최대한 멀리 라우팅
- Star grounding: PAM8403 GND와 디지털 GND를 한 점에서만 합류
- LCD 디커플링 캐패시터를 VCC 핀 최대한 가까이 배치

---

## 소프트웨어 구성

### 언어 / 환경
- Python (PEP 8), SSH + vim, Claude Code
- mpv: 스트리밍 엔진

### 모듈 구조
```
main.py
├── display.py      — ST7789 LCD 제어 (SPI0, BCM 26/13/6/12)
├── player.py       — mpv 래퍼, 스트리밍 제어, 헤드폰 감지 (BCM 23/24)
├── battery.py      — ADS1115 배터리/VBUS 모니터 (I2C1, BCM 2/3)
├── weather.py      — OpenWeatherMap API
├── input.py        — 로터리 엔코더 처리 (BCM 17/27/22)
└── config.py       — 설정 관리 (JSON)
```

### 버튼 동작 구조 (확정)

```
짧게 누르기  → 볼륨 조절 모드
  로터리 회전 → 볼륨 ±5%
  버튼 누르기 → normal 복귀
  3초 무입력  → normal 자동 복귀

길게 누르기 (1초) → 시스템 메뉴
  로터리 회전 → 항목 선택
  버튼 누르기 → 선택 실행
  3초 무입력  → normal 자동 복귀
```

### 시스템 메뉴 구조 (확정)

```
> Brightness   — 밝기 조절 (로터리 ±10%, 버튼으로 메뉴 복귀)
  Bluetooth    — 미구현 (placeholder)
  Back         — normal 복귀
```

### 부팅 / 스플래시

- WR-Radio 앱을 systemd 서비스로 등록, 부팅 시 자동 시작
- 앱 시작 직후 스플래시 이미지(240×240 PNG, `assets/splash.png`)를 LCD에 먼저 표시
- 그 사이 WiFi 연결 + mpv 초기화 진행
- 부팅 소요 시간: 약 34초 (서비스 최적화 후)

### 부팅 시간 최적화 (적용 완료)
- `cloud-init` 관련 서비스 비활성화 (cloud-init-main, cloud-init-local, cloud-init-network, cloud-final, cloud-config)
- `ModemManager` 비활성화 (셀룰러 모뎀 미사용)
- `NetworkManager-wait-online` 비활성화 (WiFi 백그라운드 연결로 전환)
- WiFi 설정: NetworkManager 직접 관리 (`/etc/NetworkManager/system-connections/`)
- ~~cloud-init 경유 WiFi~~ → nmcli로 직접 등록 완료
- 최적화 결과: 39초 → 34초

### UI 디자인 원칙 (확정)

- **배경색**: 전체 검은색 통일 (normal/volume/system/brightness 모드 모두)
- **폰트**: DejaVu Sans (Pi 기본 내장), 영문 전용 → 한글 깨짐 없음
- **부분 업데이트**: 화면 전환 시 잔상 방지를 위해 blank → 재렌더 순서 준수
- **힌트 표시**: normal 모드 하단 상시 표시 (`● Vol  ─── System`)
  - `display_radio_info()` 내부에서 렌더 → 별도 호출 불필요

### UI 원칙
- 로터리 엔코더 입력은 **실시간** 처리 필수 (LCD 업데이트와 분리)
- 조작 → **즉각 시각 피드백** → 이후 오디오 변경 (전통 라디오 방식 재현)

### 설정 파일
- JSON 포맷, 일반 사용자도 수정 가능하도록 단순하게
- 배포판에서 WiFi 초기 설정: cloud-init 방식 (첫 부팅에만 실행 후 자동 비활성화)

### 저널 영구 저장 설정
재부팅 후 이전 로그 확인을 위해 아래 설정 적용:
```bash
sudo mkdir -p /var/log/journal
sudo mkdir -p /etc/systemd/journald.conf.d
sudo tee /etc/systemd/journald.conf.d/persistent.conf << EOF
[Journal]
Storage=persistent
EOF
sudo systemctl restart systemd-journald
sudo reboot
```
이후 `journalctl -b -1`로 직전 세션 로그 확인 가능.

> ⚠️ **배포판 제작 시 주의**: SD카드에 로그를 계속 쌓으면 쓰기 횟수 증가 → SD카드 수명 단축. 개발/디버깅 용도로만 사용하고, 배포판 이미지에서는 `Storage=volatile`(메모리 저장)로 되돌릴 것.

---

## SD카드 보호: overlayfs (배포판 적용 예정)

### 개요
- 루트 파일시스템을 읽기 전용으로 마운트, 모든 변경사항은 RAM(tmpfs)에만 기록
- 전원 갑자기 끊어도 SD카드 손상 없음 → 슬라이드 스위치로 바로 전원 차단 가능
- SD카드 쓰기 없음 → 수명 사실상 무한

### 영구 저장 필요 항목 (별도 `/data` 파티션)
- config.json (볼륨, 밝기, 마지막 채널)
- Bluetooth 페어링 정보

### 개발 중 운용
- 개발 시에는 overlayfs OFF (코드 수정 필요)
- 전원 테스트 시에는 `sudo halt` 후 슬라이드 스위치 OFF
- 배포판 최종 이미지에서 overlayfs ON으로 전환

---

## PCB 설계 계획

### 형태
- Pi Zero 2W와 수평 배치 (적층 아닌 옆으로 나란히)
- Pi 쪽: 직각 male 핀헤더
- PCB 쪽: 직각 female 소켓 (2x20 right angle female header)
- 케이블 없이 직결 → SPI 노이즈 최소화, 루프 면적 최소화

### LCD 연결
- 케이스에 LCD 고정, PCB와 FFC/FPC 케이블로 연결
- 케이블 최단 길이 유지
- GND 핀을 신호선 사이사이에 배치 (크로스토크 억제)
- LCD 커넥터 주변 100nF 세라믹 캐패시터

### 그라운드 설계
- GND 플레인 전체 적용
- Star grounding: PAM8403 GND와 디지털 GND 한 점 합류
- 아날로그 GND(DAC, 앰프)와 디지털 GND 분리

### 전원 회로
- TP4056 + TPS61023 분리 구성
- DW01 보호회로 포함
- 슈퍼캡 홀드업 회로 불필요 (드롭아웃 없음)
- EN 핀 제어용 슬라이드 스위치
- VBUS 감지용 10kΩ/10kΩ 분압 → ADS1115 A1

---

## 케이스 (Fusion 360)

- 파라메트릭 Python 스크립트로 자동 생성
- 보스(boss), 리브(rib), 마운팅 시스템, 컴포넌트별 컷아웃 포함
- 대형 배터리 + 스피커 레이아웃 최적화 중
- Loft 연산으로 테이퍼 형상 구현

---

## 스피커 현황

- 현재: 102mm 4Ω 스피커 → 저음 왜곡 문제 + SPI 노이즈 심함 (PCB 이후 재평가)
- 검토 중:
  - 30mm 캡슐 스피커 (금속 메시 일체형) — 케이스 설계 단순화
  - 익사이터(surface transducer) — 스피커 마운팅 불필요

---

## 향후 계획

- [x] TP4056 + TPS61023 브레드보드 검증 완료
- [x] USB 핫스왑 드롭아웃 없음 확인
- [x] 전체 부하 테스트 (Pi + DAC + 앰프 + LCD) 완료
- [x] ADS1115 배터리 전압 + VBUS 감지 구현
- [x] 부팅 시간 최적화 (39초 → 34초)
- [x] 스플래시 이미지 + systemd 서비스 구현
- [ ] PCB 설계 (TP4056 + TPS61023 전원 구성, Pi 수평 배치, 직각 헤더 연결)
- [ ] 스피커 선정 확정 (PCB 이후 노이즈 재평가)
- [ ] 케이스 최종 설계 완성 + 출력
- [ ] overlayfs 설정 (배포판 단계)
- [ ] SD카드 이미지 배포판 제작 (의존성 포함)
- [ ] 사용자용 설정 가이드 문서화
- [ ] Bluetooth 헤드폰 지원 (선택적 확장)
- [ ] 마이크 입력 / 필드 레코딩 (장기)

---

## 주요 부품 공급처

- 디바이스마트, 아이씨뱅큐 (국내)
- Adafruit, AliExpress (해외)

---

*최종 업데이트: 2026-03-21 (전원 아키텍처 검증 완료: TP4056 + TPS61023 확정, MP2639A 불채택, 택트 스위치 제거 — 슬라이드 스위치 단독 운용, VBUS 감지 구현, 부팅 최적화 완료, 스플래시 구현)*
