# WR-Radio 설계 노트

> 새 채팅 세션 시작 시 이 파일을 붙여넣으면 맥락이 바로 복원됩니다.

---

## 프로젝트 개요

Raspberry Pi Zero 2W 기반 인터넷 라디오. 전통적인 라디오 조작감을 재현하면서 전세계 5개 지역의 환경음(ambient sound)을 스트리밍하는 휴대용 배터리 구동 기기.

**스트리밍 지역**
- Jeju Georo (제주, 한국)
- London Stave Hill (런던, 영국)
- New York Wave Farm (뉴욕, 미국)
- Jasper Ridge (캘리포니아, 미국)
- Mt. Fuji Forest (후지산, 일본)

날씨 정보(OpenWeatherMap API)를 각 지역별로 함께 표시하여 몰입감 강화.

---

## 하드웨어 구성

| 컴포넌트 | 모델 | 역할 |
|---------|------|------|
| SBC | Raspberry Pi Zero 2W | 메인 컴퓨터 |
| 디스플레이 | ST7789 240x240 LCD | UI 표시 |
| 입력 | 로터리 엔코더 | 채널/볼륨 조작 |
| DAC | UDA1334A (I2S) | 디지털→아날로그 변환 |
| 앰프 | PAM8403 | 스피커 구동 |
| 헤드폰 잭 | PJ-307C (스위치드) | 헤드폰/스피커 자동 전환 |
| 배터리 | 10000mAh LiPo | 전원 |
| 전원관리 | LiPo Rider Plus | 충전/방전 관리 |
| ADC | ADS1115 | 배터리 전압 측정 |
| 케이스 | 커스텀 3D 프린팅 (Fusion 360) | 하우징 |

---

## GPIO 핀 맵 (BCM 번호 기준)

### 전체 요약

| 모듈 | 신호 | BCM 핀 | 인터페이스 | 비고 |
|------|------|--------|-----------|------|
| **로터리 엔코더** | S1 | 17 | GPIO (PUD_UP) | |
| | S2 | 27 | GPIO (PUD_UP) | |
| | BTN | 22 | GPIO (PUD_UP) | |
| **ST7789 LCD** | CS | 26 | SPI0 CE | |
| | DC | 13 | GPIO OUT | |
| | RST | 6 | GPIO OUT | |
| | BL | 12 | PWM0 (1kHz) | 백라이트 밝기 |
| **UDA1334A** | WSEL (LRCLK) | 19 | I2S (PCM_FS) | 커널 처리 |
| | BCLK | 18 | I2S (PCM_CLK) | 커널 처리 |
| | DIN | 21 | I2S (PCM_DOUT) | 커널 처리 |
| **PAM8403** | STBY | 24 | GPIO OUT | LOW=대기, HIGH=동작 |
| **헤드폰 감지** | DETECT | 23 | GPIO IN (PUD_UP) | PJ-307C 스위치 접점 |
| **ADS1115** | SDA | 2 | I2C1 | 배터리 전압 측정 |
| | SCL | 3 | I2C1 | 배터리 전압 측정 |

### 배터리 전압 측정 회로

```
LiPo (+) ──[저항 분배기]── ADS1115 A0
                             × 2 (소프트웨어 보정)
```

- ADS1115 A0 단일 채널 사용
- 측정값 × 2 → 실제 배터리 전압
- I2C 주소: 기본값 (ADDR → GND, 0x48)

### 헤드폰 감지 동작

```
헤드폰 미삽입: DETECT = LOW  → PAM8403 STBY HIGH (앰프 ON)
헤드폰 삽입:   DETECT = HIGH → PAM8403 STBY LOW  (앰프 OFF)
```

소프트웨어(`player.py`)에서 GPIO 23 폴링으로 감지, 50ms 디바운스 적용.

---

## 전원 시스템

### 기본 구성
```
USB 외부전원 또는 LiPo 배터리
    └── LiPo Rider Plus
            ├── EN 핀 제어로 전원 ON/OFF
            │     EN핀 → GND : 출력 OFF
            │     EN핀 → OPEN : 출력 ON
            └── 5V 출력 → Pi + 주변기기
```

### 전원 스위치 구성 (DPDT)

DPDT 토글 스위치로 EN 핀 제어와 슈퍼캡 회로를 동시에 제어:

```
DPDT 토글 스위치
├── 극 1: EN 핀 제어
│     COM → EN 핀
│     NO  → OPEN  (스위치 ON 시 EN 플로팅 → LiPo Rider 출력 ON)
│     NC  → GND   (스위치 OFF 시 EN → GND → LiPo Rider 출력 OFF)
│
└── 극 2: 슈퍼캡 회로 제어
      COM → LiPo Rider 5V
      NO  → [20Ω // 다이오드] → 슈퍼캡 (+)  (스위치 ON 시 충전 연결)
      NC  → [10Ω] → GND  (스위치 OFF 시 슈퍼캡 빠른 방전)
```

**슈퍼캡 방전 동작:**
- 스위치 OFF → NC 접점 닫힘 → 슈퍼캡 → 10Ω → GND → 빠른 방전
- 시정수 = 10Ω × 2.5F = 25초
- LiPo Rider 재시작 시 슈퍼캡 잔류 전하 간섭 없음 → 바로 재켜짐 가능

### 슈퍼캡 홀드업 회로

```
LiPo Rider 5V ──┬──────────────────── Pi VCC (직결)
                └──[20Ω // 다이오드]── 슈퍼캡 (+)
                                       슈퍼캡 (-) → GND
```

- **20Ω**: 인러시 전류 제한 (충전 시), 1/4W × 2개 직렬로 구성
- **다이오드 (1N5819)**: 20Ω과 병렬, 방전 시 저항 없이 빠르게 Pi에 공급
- **슈퍼캡 용량**: 2.5F
- Pi는 저항 앞에 직결 → 저항값과 무관하게 전원 공급

### 전원 안정화
- Pi 메인 전원 라인: **페라이트 비드 제거** (고전류 라인 부적합, WiFi 피크 전류 시 전압강하 유발)
- Pi 전원 바로 옆: **220µF 캐패시터** (부팅 피크 전류 버퍼링)

### 배터리 관리 원칙
- 최적 사용 범위: **20~80% 충전** 유지 (수명 3배 연장)
- LiPo는 완전 방전/완전 충전 반복이 수명에 해로움 (NiCd와 반대)

---

## 오디오 시스템

### 신호 경로
```
Pi (I2S) → UDA1334A DAC → [전압 분배기] → PJ-307C 잭
                                              ├── 헤드폰 (직결)
                                              └── PAM8403 앰프 → 스피커
```

### UDA1334A 연결
- VIN: 5V (온보드 레귤레이터가 3.3V로 변환)
- 3VO: 모듈 출력 3.3V (다른 부품 공급용)
- `config.txt`에 `dtoverlay=hifiberry-dac` 적용
- `aplay -l` 에서 `card 0: sndrpihifiberry` 로 인식 확인

### 헤드폰/스피커 자동 전환
- PJ-307C의 물리적 스위치 접점 이용 (소프트웨어 불필요)
- 헤드폰 삽입 시 스피커 회로 자동 차단

### 노이즈 대책 (중요)
- 헤드폰 출력: **AGND** 연결로 디지털 노이즈 최소화
- 앰프 GND와 DAC AGND 분리 (그라운드 오염 방지)
- 전압 분배기: 클리핑 방지용

### 페라이트 비드 + 캐패시터 필터

**위치 우선순위**
1. UDA1334A VIN (DAC 전원) — 오디오 품질 직결, 최우선
2. PAM8403 VCC (앰프 전원) — 스피커 험 억제
3. LCD 전원 — LCD 스위칭 노이즈 차단

```
5V ──[페라이트 비드]──┬──── VIN (DAC/앰프)
                     ├── 10µF 전해/탄탈 (저주파 안정화)
                     └── 100nF 세라믹 (고주파 바이패스)
                     │
                    GND
```

**주의**: 페라이트 비드는 DC 저항 있음 → Pi 메인 전원 같은 고전류 라인엔 부적합.

---

## LCD SPI 노이즈 문제 (미해결 → PCB에서 근본 해결 예정)

### 현상
- 프로그램 실행 중 LCD 백라이트 밝기 출렁거림
- 스피커에서 디지털 노이즈 (헤드폰은 깨끗함 → DAC는 무관, PAM8403 경로 문제)
- **LCD를 끄면 노이즈 완전히 사라짐 → SPI 전송이 주범 확정**

### 현재 적용한 완화책 (소프트웨어)
- SPI 속도 64MHz → **16MHz** 낮춤
- 애니메이션 업데이트 주기 늘림

### 현재 적용한 완화책 (하드웨어)
- LCD VCC에 100nF 세라믹 캐패시터 → GND
- PAM8403 VCC에 페라이트 비드 + 100µF + 100nF

### PCB 설계 시 근본 해결 포인트
- GND 플레인 전체 깔기 (루프 면적 최소화)
- SPI 라인(BCM 26/13/6)과 아날로그 신호선 최대한 멀리 라우팅
- Star grounding: PAM8403 GND와 디지털 GND를 한 점에서만 합류
- LCD 디커플링 캐패시터를 VCC 핀 최대한 가까이 배치

---

## 소프트웨어 구성

### 언어 / 환경
- Python (PEP 8), SSH + vim
- mpv: 스트리밍 엔진

### 모듈 구조
```
main.py
├── display.py      — ST7789 LCD 제어 (SPI0, BCM 26/13/6/12)
├── player.py       — mpv 래퍼, 스트리밍 제어, 헤드폰 감지 (BCM 23/24)
├── battery.py      — ADS1115 배터리 모니터 (I2C1, BCM 2/3)
├── weather.py      — OpenWeatherMap API
├── input.py        — 로터리 엔코더 처리 (BCM 17/27/22)
└── config.py       — 설정 관리 (JSON)
```

### UI 원칙
- 로터리 엔코더 입력은 **실시간** 처리 필수 (LCD 업데이트와 분리)
- 조작 → **즉각 시각 피드백** → 이후 오디오 변경 (전통 라디오 방식 재현)

### 설정 파일
- JSON 포맷, 일반 사용자도 수정 가능하도록 단순하게
- boot 파티션 cloud-init 방식 (network-config, user-data)
- WiFi 설정: `/boot/network-config` (netplan 포맷)

---

## PCB 설계 계획

### 형태
- Pi Zero 2W와 수평 배치 (적층 아닌 옆으로 나란히)
- Pi 쪽: 직각 male 핀헤더
- PCB 쪽: 직각 female 소켓 (2x20 right angle female header)
- 케이블 없이 직결 → SPI 노이즈 최소화, 루프 면적 최소화

### LCD 연결
- 케이스에 LCD 고정, PCB와 FFC/FPC 케이블로 연결
- 케이블 최단 길이 유지
- GND 핀을 신호선 사이사이에 배치 (크로스토크 억제)
- LCD 커넥터 주변 100nF 세라믹 캐패시터

### 그라운드 설계
- GND 플레인 전체 적용
- Star grounding: PAM8403 GND와 디지털 GND 한 점 합류
- 아날로그 GND(DAC, 앰프)와 디지털 GND 분리

---

## 케이스 (Fusion 360)

- 파라메트릭 Python 스크립트로 자동 생성
- 보스(boss), 리브(rib), 마운팅 시스템, 컴포넌트별 컷아웃 포함
- 대형 배터리 + 스피커 레이아웃 최적화 중
- Loft 연산으로 테이퍼 형상 구현

---

## 스피커 현황

- 현재: 102mm 4Ω 스피커 → 저음 왜곡 문제
- 검토 중:
  - 30mm 캡슐 스피커 (금속 메시 일체형) — 케이스 설계 단순화
  - 익사이터(surface transducer) — 스피커 마운팅 불필요

---

## 향후 계획

- [ ] 이미지 백업 (현재 상태 스냅샷)
- [ ] DPDT 슈퍼캡 전원 제어 최종 검증
- [ ] 케이스 최종 설계 완성 + 출력
- [ ] 스피커 선정 확정
- [ ] PCB 설계 (Pi 수평 배치, 직각 헤더 연결)
- [ ] SD카드 이미지 배포판 제작 (의존성 포함)
- [ ] 사용자용 설정 가이드 문서화
- [ ] Bluetooth 헤드폰 지원 (선택적 확장)
- [ ] 마이크 입력 / 필드 레코딩 (장기)

---

## 주요 부품 공급처

- 디바이스마트, 아이씨뱅큐 (국내)
- Adafruit, AliExpress (해외)

---

*최종 업데이트: 2026-03-04 (DPDT 전원 스위치 구성, 슈퍼캡 방전 회로, Pi 전원 안정화, PCB 설계 계획 추가)*
